// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: hi/ai/plugin/base.proto

// ai 的**插件执行子服务**内部面(backend-hi-ai-plugin 实现)。
//
// 位置即语义:放在 `hi/ai/plugin/` 而不是顶层 —— plugin 是 ai 的子服务,**不是**与 ai/club/did
// 平级的模块。包名 `hi.ai.plugin` 同理。与 `hi/club/trade/` 一套范式。
//
// ## 与对外面 `hi.ai.Plugin` 的分工
//
// `hi.ai.Plugin`(AUTH_MERCHANT,backend-hi-ai 实现)是**插件管理**:建壳、发版本、增删改查、
// 开关与激活 —— 面向 hiai web 与三方商户后台,有明确的主体(商户)。
// 本文件是**执行契约**:给定脚本包与参数,跑一次、清理产物。它**没有"谁在调"这个概念**,
// 也不需要有 —— 主体校验在 ai 那一层做完了。
//
// ## 为什么单独拆出来
//
// 原来 `service AiPlugin` 与对外的 `service Plugin` 挤在同一个文件、同一个包(`hi.ai`)里,
// 后果有三:
//   1. **档位标错**:Run/Cleanup 原标 `AUTH_NONE`(= 公开:真·免鉴权),但它只由 ai 转发调用,
//      是内部面。`AUTH_NONE` 会诱导人把它对外暴露 —— 而它执行的是**任意代码**
//      (code_archive_url 指哪跑哪),暴露出去后果比一般接口严重得多。现改标 AUTH_INTERNAL。
//   2. **服务名冗余**:`hi.ai.AiPlugin` 里 "Ai" 重复了一遍。这个别扭名字正是"没地方放"憋出来的
//      —— 不能叫 Plugin(被对外面占了),只好加前缀。搬到本包后改名 `Runner`,
//      全名 `hi.ai.plugin.Runner`,同时更准确地描述"跑脚本的那个东西"。
//   3. **结构不体现从属**:子服务的内部契约混在父服务的对外文件里,看不出谁是谁的子服务。
//
// 对外契约**零变化**:`hi.ai.Plugin` 那 14 个管理方法一个不动,前端与 hiai web 无感。

package plugin

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Runner_Run_FullMethodName     = "/hi.ai.plugin.Runner/Run"
	Runner_Cleanup_FullMethodName = "/hi.ai.plugin.Runner/Cleanup"
)

// RunnerClient is the client API for Runner service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// 脚本执行器(内部面)。只由父服务 ai 经 grpc 转发调用;不鉴权、也不认识"调用者"
// —— 主体(商户/机器人)的校验在 `hi.ai.Plugin` 那一层完成。
type RunnerClient interface {
	Run(ctx context.Context, in *RunReq, opts ...grpc.CallOption) (*RunResp, error)
	Cleanup(ctx context.Context, in *CleanupReq, opts ...grpc.CallOption) (*emptypb.Empty, error)
}

type runnerClient struct {
	cc grpc.ClientConnInterface
}

func NewRunnerClient(cc grpc.ClientConnInterface) RunnerClient {
	return &runnerClient{cc}
}

func (c *runnerClient) Run(ctx context.Context, in *RunReq, opts ...grpc.CallOption) (*RunResp, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(RunResp)
	err := c.cc.Invoke(ctx, Runner_Run_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *runnerClient) Cleanup(ctx context.Context, in *CleanupReq, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, Runner_Cleanup_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// RunnerServer is the server API for Runner service.
// All implementations should embed UnimplementedRunnerServer
// for forward compatibility.
//
// 脚本执行器(内部面)。只由父服务 ai 经 grpc 转发调用;不鉴权、也不认识"调用者"
// —— 主体(商户/机器人)的校验在 `hi.ai.Plugin` 那一层完成。
type RunnerServer interface {
	Run(context.Context, *RunReq) (*RunResp, error)
	Cleanup(context.Context, *CleanupReq) (*emptypb.Empty, error)
}

// UnimplementedRunnerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedRunnerServer struct{}

func (UnimplementedRunnerServer) Run(context.Context, *RunReq) (*RunResp, error) {
	return nil, status.Error(codes.Unimplemented, "method Run not implemented")
}
func (UnimplementedRunnerServer) Cleanup(context.Context, *CleanupReq) (*emptypb.Empty, error) {
	return nil, status.Error(codes.Unimplemented, "method Cleanup not implemented")
}
func (UnimplementedRunnerServer) testEmbeddedByValue() {}

// UnsafeRunnerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to RunnerServer will
// result in compilation errors.
type UnsafeRunnerServer interface {
	mustEmbedUnimplementedRunnerServer()
}

func RegisterRunnerServer(s grpc.ServiceRegistrar, srv RunnerServer) {
	// If the following call panics, it indicates UnimplementedRunnerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Runner_ServiceDesc, srv)
}

func _Runner_Run_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(RunReq)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(RunnerServer).Run(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Runner_Run_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(RunnerServer).Run(ctx, req.(*RunReq))
	}
	return interceptor(ctx, in, info, handler)
}

func _Runner_Cleanup_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(CleanupReq)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(RunnerServer).Cleanup(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Runner_Cleanup_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(RunnerServer).Cleanup(ctx, req.(*CleanupReq))
	}
	return interceptor(ctx, in, info, handler)
}

// Runner_ServiceDesc is the grpc.ServiceDesc for Runner service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Runner_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "hi.ai.plugin.Runner",
	HandlerType: (*RunnerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Run",
			Handler:    _Runner_Run_Handler,
		},
		{
			MethodName: "Cleanup",
			Handler:    _Runner_Cleanup_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "hi/ai/plugin/base.proto",
}

const (
	Builder_Build_FullMethodName     = "/hi.ai.plugin.Builder/Build"
	Builder_VerifyLua_FullMethodName = "/hi.ai.plugin.Builder/VerifyLua"
)

// BuilderClient is the client API for Builder service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// 设备端插件的**产出与验收**(内部面)。只由父服务 ai 经 grpc 转发调用。
//
// ⚠️ **它执行的是三方代码** —— `Build` 跑三方的 `build.rs` 与依赖的构建脚本,
// `VerifyLua` 把三方脚本真 load 一遍。比 Runner 跑 py 脚本危险程度只高不低。
// 必须在容器里跑,且容器内不得挂载任何宿主凭证(ssh key / gitea token)。
type BuilderClient interface {
	Build(ctx context.Context, in *BuildReq, opts ...grpc.CallOption) (*BuildResp, error)
	// 验一个 lua 包:语法能不能 load、manifest 是什么、契约号多少、有没有碰禁用项。
	// 毫秒级,hi-ai 在发版流程里同步等。
	VerifyLua(ctx context.Context, in *VerifyLuaReq, opts ...grpc.CallOption) (*VerifyLuaResp, error)
}

type builderClient struct {
	cc grpc.ClientConnInterface
}

func NewBuilderClient(cc grpc.ClientConnInterface) BuilderClient {
	return &builderClient{cc}
}

func (c *builderClient) Build(ctx context.Context, in *BuildReq, opts ...grpc.CallOption) (*BuildResp, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BuildResp)
	err := c.cc.Invoke(ctx, Builder_Build_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *builderClient) VerifyLua(ctx context.Context, in *VerifyLuaReq, opts ...grpc.CallOption) (*VerifyLuaResp, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(VerifyLuaResp)
	err := c.cc.Invoke(ctx, Builder_VerifyLua_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// BuilderServer is the server API for Builder service.
// All implementations should embed UnimplementedBuilderServer
// for forward compatibility.
//
// 设备端插件的**产出与验收**(内部面)。只由父服务 ai 经 grpc 转发调用。
//
// ⚠️ **它执行的是三方代码** —— `Build` 跑三方的 `build.rs` 与依赖的构建脚本,
// `VerifyLua` 把三方脚本真 load 一遍。比 Runner 跑 py 脚本危险程度只高不低。
// 必须在容器里跑,且容器内不得挂载任何宿主凭证(ssh key / gitea token)。
type BuilderServer interface {
	Build(context.Context, *BuildReq) (*BuildResp, error)
	// 验一个 lua 包:语法能不能 load、manifest 是什么、契约号多少、有没有碰禁用项。
	// 毫秒级,hi-ai 在发版流程里同步等。
	VerifyLua(context.Context, *VerifyLuaReq) (*VerifyLuaResp, error)
}

// UnimplementedBuilderServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedBuilderServer struct{}

func (UnimplementedBuilderServer) Build(context.Context, *BuildReq) (*BuildResp, error) {
	return nil, status.Error(codes.Unimplemented, "method Build not implemented")
}
func (UnimplementedBuilderServer) VerifyLua(context.Context, *VerifyLuaReq) (*VerifyLuaResp, error) {
	return nil, status.Error(codes.Unimplemented, "method VerifyLua not implemented")
}
func (UnimplementedBuilderServer) testEmbeddedByValue() {}

// UnsafeBuilderServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to BuilderServer will
// result in compilation errors.
type UnsafeBuilderServer interface {
	mustEmbedUnimplementedBuilderServer()
}

func RegisterBuilderServer(s grpc.ServiceRegistrar, srv BuilderServer) {
	// If the following call panics, it indicates UnimplementedBuilderServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Builder_ServiceDesc, srv)
}

func _Builder_Build_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(BuildReq)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(BuilderServer).Build(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Builder_Build_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(BuilderServer).Build(ctx, req.(*BuildReq))
	}
	return interceptor(ctx, in, info, handler)
}

func _Builder_VerifyLua_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(VerifyLuaReq)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(BuilderServer).VerifyLua(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Builder_VerifyLua_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(BuilderServer).VerifyLua(ctx, req.(*VerifyLuaReq))
	}
	return interceptor(ctx, in, info, handler)
}

// Builder_ServiceDesc is the grpc.ServiceDesc for Builder service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Builder_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "hi.ai.plugin.Builder",
	HandlerType: (*BuilderServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Build",
			Handler:    _Builder_Build_Handler,
		},
		{
			MethodName: "VerifyLua",
			Handler:    _Builder_VerifyLua_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "hi/ai/plugin/base.proto",
}
