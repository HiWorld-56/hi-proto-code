// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: hi/club/ledger.proto

package club

import (
	context "context"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Ledger_Record_FullMethodName = "/hi.club.Ledger/Record"
	Ledger_List_FullMethodName   = "/hi.club.Ledger/List"
)

// LedgerClient is the client API for Ledger service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ── 机器人的资金流水 ─────────────────────────────────────────────────────────
//
// **只装非市场支出。** 市场订单(买插件、续费)有自己的一份 `MarketPayment`,
// 那份挂在订单上、由认款流程写。同一笔记两处必然漂 —— 一处改了另一处不会知道。
// 页面上要"一张总表"就在**上层合并两个来源**,不要在库里存第二份。
//
// 第一种是**提款**:硬件机器人把自己收到的钱结算给主人。它不经市场、不产生订单,
// 在此之前**全网没有任何地方看得到它** —— 出了事只能上链去翻。
//
// ## 为什么由机器人自己报
//
// 付款方是机器人(私钥在它手里,club 全程碰不到钱),所以只有它知道"这笔发出去了"。
// 用的是它自己的已认证通道 —— `payer` 天然就是它,不接受入参
// (让调用方传付款人,等于给自己开一个"以别人的名义记账"的口子)。
//
// ## 它是**留痕**,不是判据
//
// 防重复付款那本账在机器人本地(sqlite,先落意向再动钱)。本表是给人看的:
// 上报失败不能让钱卡住,所以机器人是**攒着补报**的 —— 于是同一条可能报两次,
// 靠 `uuid` 幂等。**别拿本表当"付没付过"的依据**:它可能迟到,也可能永远不到
// (机器人在报出去之前就下线了)。
type LedgerClient interface {
	// 机器人上报一笔自己花出去的钱。**幂等**:同 uuid 重复上报只更新,不新增。
	Record(ctx context.Context, in *FundsRecord, opts ...grpc.CallOption) (*emptypb.Empty, error)
	// 查流水。空 did = 看我自己;填了则**必须是我的仆从机器人**。
	List(ctx context.Context, in *ListFundsReq, opts ...grpc.CallOption) (*ListFundsResp, error)
}

type ledgerClient struct {
	cc grpc.ClientConnInterface
}

func NewLedgerClient(cc grpc.ClientConnInterface) LedgerClient {
	return &ledgerClient{cc}
}

func (c *ledgerClient) Record(ctx context.Context, in *FundsRecord, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, Ledger_Record_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *ledgerClient) List(ctx context.Context, in *ListFundsReq, opts ...grpc.CallOption) (*ListFundsResp, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(ListFundsResp)
	err := c.cc.Invoke(ctx, Ledger_List_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// LedgerServer is the server API for Ledger service.
// All implementations should embed UnimplementedLedgerServer
// for forward compatibility.
//
// ── 机器人的资金流水 ─────────────────────────────────────────────────────────
//
// **只装非市场支出。** 市场订单(买插件、续费)有自己的一份 `MarketPayment`,
// 那份挂在订单上、由认款流程写。同一笔记两处必然漂 —— 一处改了另一处不会知道。
// 页面上要"一张总表"就在**上层合并两个来源**,不要在库里存第二份。
//
// 第一种是**提款**:硬件机器人把自己收到的钱结算给主人。它不经市场、不产生订单,
// 在此之前**全网没有任何地方看得到它** —— 出了事只能上链去翻。
//
// ## 为什么由机器人自己报
//
// 付款方是机器人(私钥在它手里,club 全程碰不到钱),所以只有它知道"这笔发出去了"。
// 用的是它自己的已认证通道 —— `payer` 天然就是它,不接受入参
// (让调用方传付款人,等于给自己开一个"以别人的名义记账"的口子)。
//
// ## 它是**留痕**,不是判据
//
// 防重复付款那本账在机器人本地(sqlite,先落意向再动钱)。本表是给人看的:
// 上报失败不能让钱卡住,所以机器人是**攒着补报**的 —— 于是同一条可能报两次,
// 靠 `uuid` 幂等。**别拿本表当"付没付过"的依据**:它可能迟到,也可能永远不到
// (机器人在报出去之前就下线了)。
type LedgerServer interface {
	// 机器人上报一笔自己花出去的钱。**幂等**:同 uuid 重复上报只更新,不新增。
	Record(context.Context, *FundsRecord) (*emptypb.Empty, error)
	// 查流水。空 did = 看我自己;填了则**必须是我的仆从机器人**。
	List(context.Context, *ListFundsReq) (*ListFundsResp, error)
}

// UnimplementedLedgerServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedLedgerServer struct{}

func (UnimplementedLedgerServer) Record(context.Context, *FundsRecord) (*emptypb.Empty, error) {
	return nil, status.Error(codes.Unimplemented, "method Record not implemented")
}
func (UnimplementedLedgerServer) List(context.Context, *ListFundsReq) (*ListFundsResp, error) {
	return nil, status.Error(codes.Unimplemented, "method List not implemented")
}
func (UnimplementedLedgerServer) testEmbeddedByValue() {}

// UnsafeLedgerServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to LedgerServer will
// result in compilation errors.
type UnsafeLedgerServer interface {
	mustEmbedUnimplementedLedgerServer()
}

func RegisterLedgerServer(s grpc.ServiceRegistrar, srv LedgerServer) {
	// If the following call panics, it indicates UnimplementedLedgerServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Ledger_ServiceDesc, srv)
}

func _Ledger_Record_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(FundsRecord)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(LedgerServer).Record(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Ledger_Record_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(LedgerServer).Record(ctx, req.(*FundsRecord))
	}
	return interceptor(ctx, in, info, handler)
}

func _Ledger_List_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(ListFundsReq)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(LedgerServer).List(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Ledger_List_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(LedgerServer).List(ctx, req.(*ListFundsReq))
	}
	return interceptor(ctx, in, info, handler)
}

// Ledger_ServiceDesc is the grpc.ServiceDesc for Ledger service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Ledger_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "hi.club.Ledger",
	HandlerType: (*LedgerServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Record",
			Handler:    _Ledger_Record_Handler,
		},
		{
			MethodName: "List",
			Handler:    _Ledger_List_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "hi/club/ledger.proto",
}
