// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: hi/club/callback.proto

package club

import (
	context "context"
	hi "github.com/HiWorld-56/hi-proto/go/hi"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	MarketProvider_Begin_FullMethodName  = "/hi.club.MarketProvider/Begin"
	MarketProvider_Cancel_FullMethodName = "/hi.club.MarketProvider/Cancel"
)

// MarketProviderClient is the client API for MarketProvider service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// ── 插件市场的认证流程契约:**club 只定义,由挂牌方的后台实现** ──────────────────
//
// 照 `hi/did/callback.proto` 的范式:did 定义 `LoginCallback` / `PayCallback`,
// 由三方业务(club 等)实现并注册到自己的服务里,**did 侧没有 handler 是正常的**。
// 这里同理 —— club 侧不实现这两个方法,它们是被商户实现的;club 是**调用方**。
//
// ## 为什么用 gRPC 而不是 webhook
//
// 数据结构在 hi-proto 一次定义、两边共用生成码,字段改了编译期就知道;
// 不用另写一份 webhook 文档,也不会出现「文档写了实现没跟」的漂移。
// 代价是接入方得能起 gRPC 服务 —— 而典型场景里接入方本来就是**注册商户**
// (`MerchantInfo.endpoint` 就是它的后台地址),这个门槛本来就得跨。
//
// ## 为什么鉴权是 AUTH_WEB3
//
// club 手里没有商户的 token,传输层无从鉴权;但载荷是 web3 签名的,伪造不了。
// 典型链路:
//
//	注册 hisrv 商户 A → 注册 hiclub 账号 → 创建机器人 B → 插件全挂 B 上
//	  → 用户为其机器人购买 → 商户 A 调 hi.did.Merchant.AddUsers(购买者 did) 纳入私域
//	  → 结算走 hidid(收款方 = MerchantPubServerResp.server,默认 = master)
//
// 接入方本来就是注册商户、天然持 did 私钥,`AUTH_WEB3` 直接成立,与 hidid 的回调同一套验签。
// **不需要第二套 HMAC 密钥体系。**
//
// ⚠️ 「把购买者纳入私域」这一步**由商户 A 自己做,不是 club 代做** ——
//
//	`Merchant.AddUsers` 的主体由 ExtendToken 解出「加到自己名下」,
//	club 手里只有 club 自己的商户凭证,代调只会把人加到 club 名下、够不着商户 A。
//	club 的职责到「把 `to_master` 放进 MarketBeginData 告诉商户 A」为止。这个边界别越。
type MarketProviderClient interface {
	// Begin club 受理申请 → 通知商户后台开始业务流程(收款 / 审核)。
	Begin(ctx context.Context, in *hi.SignedData, opts ...grpc.CallOption) (*BeginResp, error)
	// Cancel 授权被撤销 / 到期 → 通知商户后台(退款、清私域等由商户自理)。
	Cancel(ctx context.Context, in *hi.SignedData, opts ...grpc.CallOption) (*emptypb.Empty, error)
}

type marketProviderClient struct {
	cc grpc.ClientConnInterface
}

func NewMarketProviderClient(cc grpc.ClientConnInterface) MarketProviderClient {
	return &marketProviderClient{cc}
}

func (c *marketProviderClient) Begin(ctx context.Context, in *hi.SignedData, opts ...grpc.CallOption) (*BeginResp, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(BeginResp)
	err := c.cc.Invoke(ctx, MarketProvider_Begin_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

func (c *marketProviderClient) Cancel(ctx context.Context, in *hi.SignedData, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, MarketProvider_Cancel_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// MarketProviderServer is the server API for MarketProvider service.
// All implementations should embed UnimplementedMarketProviderServer
// for forward compatibility.
//
// ── 插件市场的认证流程契约:**club 只定义,由挂牌方的后台实现** ──────────────────
//
// 照 `hi/did/callback.proto` 的范式:did 定义 `LoginCallback` / `PayCallback`,
// 由三方业务(club 等)实现并注册到自己的服务里,**did 侧没有 handler 是正常的**。
// 这里同理 —— club 侧不实现这两个方法,它们是被商户实现的;club 是**调用方**。
//
// ## 为什么用 gRPC 而不是 webhook
//
// 数据结构在 hi-proto 一次定义、两边共用生成码,字段改了编译期就知道;
// 不用另写一份 webhook 文档,也不会出现「文档写了实现没跟」的漂移。
// 代价是接入方得能起 gRPC 服务 —— 而典型场景里接入方本来就是**注册商户**
// (`MerchantInfo.endpoint` 就是它的后台地址),这个门槛本来就得跨。
//
// ## 为什么鉴权是 AUTH_WEB3
//
// club 手里没有商户的 token,传输层无从鉴权;但载荷是 web3 签名的,伪造不了。
// 典型链路:
//
//	注册 hisrv 商户 A → 注册 hiclub 账号 → 创建机器人 B → 插件全挂 B 上
//	  → 用户为其机器人购买 → 商户 A 调 hi.did.Merchant.AddUsers(购买者 did) 纳入私域
//	  → 结算走 hidid(收款方 = MerchantPubServerResp.server,默认 = master)
//
// 接入方本来就是注册商户、天然持 did 私钥,`AUTH_WEB3` 直接成立,与 hidid 的回调同一套验签。
// **不需要第二套 HMAC 密钥体系。**
//
// ⚠️ 「把购买者纳入私域」这一步**由商户 A 自己做,不是 club 代做** ——
//
//	`Merchant.AddUsers` 的主体由 ExtendToken 解出「加到自己名下」,
//	club 手里只有 club 自己的商户凭证,代调只会把人加到 club 名下、够不着商户 A。
//	club 的职责到「把 `to_master` 放进 MarketBeginData 告诉商户 A」为止。这个边界别越。
type MarketProviderServer interface {
	// Begin club 受理申请 → 通知商户后台开始业务流程(收款 / 审核)。
	Begin(context.Context, *hi.SignedData) (*BeginResp, error)
	// Cancel 授权被撤销 / 到期 → 通知商户后台(退款、清私域等由商户自理)。
	Cancel(context.Context, *hi.SignedData) (*emptypb.Empty, error)
}

// UnimplementedMarketProviderServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedMarketProviderServer struct{}

func (UnimplementedMarketProviderServer) Begin(context.Context, *hi.SignedData) (*BeginResp, error) {
	return nil, status.Error(codes.Unimplemented, "method Begin not implemented")
}
func (UnimplementedMarketProviderServer) Cancel(context.Context, *hi.SignedData) (*emptypb.Empty, error) {
	return nil, status.Error(codes.Unimplemented, "method Cancel not implemented")
}
func (UnimplementedMarketProviderServer) testEmbeddedByValue() {}

// UnsafeMarketProviderServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to MarketProviderServer will
// result in compilation errors.
type UnsafeMarketProviderServer interface {
	mustEmbedUnimplementedMarketProviderServer()
}

func RegisterMarketProviderServer(s grpc.ServiceRegistrar, srv MarketProviderServer) {
	// If the following call panics, it indicates UnimplementedMarketProviderServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&MarketProvider_ServiceDesc, srv)
}

func _MarketProvider_Begin_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(hi.SignedData)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(MarketProviderServer).Begin(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: MarketProvider_Begin_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(MarketProviderServer).Begin(ctx, req.(*hi.SignedData))
	}
	return interceptor(ctx, in, info, handler)
}

func _MarketProvider_Cancel_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(hi.SignedData)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(MarketProviderServer).Cancel(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: MarketProvider_Cancel_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(MarketProviderServer).Cancel(ctx, req.(*hi.SignedData))
	}
	return interceptor(ctx, in, info, handler)
}

// MarketProvider_ServiceDesc is the grpc.ServiceDesc for MarketProvider service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var MarketProvider_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "hi.club.MarketProvider",
	HandlerType: (*MarketProviderServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Begin",
			Handler:    _MarketProvider_Begin_Handler,
		},
		{
			MethodName: "Cancel",
			Handler:    _MarketProvider_Cancel_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "hi/club/callback.proto",
}
