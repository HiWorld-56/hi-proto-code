// Code generated by protoc-gen-go-grpc. DO NOT EDIT.
// versions:
// - protoc-gen-go-grpc v1.6.2
// - protoc             (unknown)
// source: hi/did/messaging.proto

package did

import (
	context "context"
	hi "github.com/HiWorld-56/hi-proto/go/hi"
	grpc "google.golang.org/grpc"
	codes "google.golang.org/grpc/codes"
	status "google.golang.org/grpc/status"
	emptypb "google.golang.org/protobuf/types/known/emptypb"
)

// This is a compile-time assertion to ensure that this generated file
// is compatible with the grpc package it is being compiled against.
// Requires gRPC-Go v1.64.0 or later.
const _ = grpc.SupportPackageIsVersion9

const (
	Notify_Transaction_FullMethodName = "/hi.did.Notify/Transaction"
)

// NotifyClient is the client API for Notify service.
//
// For semantics around ctx use and closing/ending streaming RPCs, please refer to https://pkg.go.dev/google.golang.org/grpc/?tab=doc#ClientConn.NewStream.
//
// Notify —— 付款方付完之后,叫 hidid 后台替它通知收款方。
//
// ## 为什么在后台发,而不是付款方自己发 MQTT
//
// 付款方**不一定连着 MQTT**:被三方唤起(deep link)去付款的那条路,
// 拉起来就是付款页,压根没走过登录+连 broker 的流程,通知发不出去。
// 而"付款成功"有好几条入口(钱包内转账 / 三方唤起 / 内置 webview 内支付),
// 各自记得发一次是靠不住的 —— 收口成一个方法,新增入口不会漏。
//
// 挪到后台之后,broker 的 `common:wacls` 就能把
// **"任何认证客户端都能写别人的 `hidid/v1/single/+`"** 这条口子关掉。
//
// ## 这不是账目,是提醒
//
// 不落库、不重试、**收款方不在线就丢**(客户端一律 clean session,broker 不给离线的存)。
// 谁都不会只凭这条通知认款 —— 要认款有 `Transfer.TxDetail` / `Transfer.VerifyTransaction`。
type NotifyClient interface {
	// 载荷 = `hi.did.Transaction` 的 protojson 文本(与本仓其它验签接口同形;
	// 验签走 FFI,那条通道只吃 UTF-8 文本,塞二进制会在第一个 NUL 处被截断)。
	//
	// ⚠️ `Transaction.from` **不收入参** —— 一律取自验签认出的签名者。
	//
	//	收了就等于让任何人冒名发付款通知。
	//
	// ⚠️ `Transaction.to.did` 必填:本生态业务层只认 did,链地址由后端按币种所在链查。
	//
	// 档位 AUTH_WEB3 与整条付款路径一致:付款方是**钱包**,它天然会签名,
	// 但不一定有 hidid 登录态(hidid-simple-app 与 hinj-brain 都只有钱包、没有 token)。
	Transaction(ctx context.Context, in *hi.SignedData, opts ...grpc.CallOption) (*emptypb.Empty, error)
}

type notifyClient struct {
	cc grpc.ClientConnInterface
}

func NewNotifyClient(cc grpc.ClientConnInterface) NotifyClient {
	return &notifyClient{cc}
}

func (c *notifyClient) Transaction(ctx context.Context, in *hi.SignedData, opts ...grpc.CallOption) (*emptypb.Empty, error) {
	cOpts := append([]grpc.CallOption{grpc.StaticMethod()}, opts...)
	out := new(emptypb.Empty)
	err := c.cc.Invoke(ctx, Notify_Transaction_FullMethodName, in, out, cOpts...)
	if err != nil {
		return nil, err
	}
	return out, nil
}

// NotifyServer is the server API for Notify service.
// All implementations should embed UnimplementedNotifyServer
// for forward compatibility.
//
// Notify —— 付款方付完之后,叫 hidid 后台替它通知收款方。
//
// ## 为什么在后台发,而不是付款方自己发 MQTT
//
// 付款方**不一定连着 MQTT**:被三方唤起(deep link)去付款的那条路,
// 拉起来就是付款页,压根没走过登录+连 broker 的流程,通知发不出去。
// 而"付款成功"有好几条入口(钱包内转账 / 三方唤起 / 内置 webview 内支付),
// 各自记得发一次是靠不住的 —— 收口成一个方法,新增入口不会漏。
//
// 挪到后台之后,broker 的 `common:wacls` 就能把
// **"任何认证客户端都能写别人的 `hidid/v1/single/+`"** 这条口子关掉。
//
// ## 这不是账目,是提醒
//
// 不落库、不重试、**收款方不在线就丢**(客户端一律 clean session,broker 不给离线的存)。
// 谁都不会只凭这条通知认款 —— 要认款有 `Transfer.TxDetail` / `Transfer.VerifyTransaction`。
type NotifyServer interface {
	// 载荷 = `hi.did.Transaction` 的 protojson 文本(与本仓其它验签接口同形;
	// 验签走 FFI,那条通道只吃 UTF-8 文本,塞二进制会在第一个 NUL 处被截断)。
	//
	// ⚠️ `Transaction.from` **不收入参** —— 一律取自验签认出的签名者。
	//
	//	收了就等于让任何人冒名发付款通知。
	//
	// ⚠️ `Transaction.to.did` 必填:本生态业务层只认 did,链地址由后端按币种所在链查。
	//
	// 档位 AUTH_WEB3 与整条付款路径一致:付款方是**钱包**,它天然会签名,
	// 但不一定有 hidid 登录态(hidid-simple-app 与 hinj-brain 都只有钱包、没有 token)。
	Transaction(context.Context, *hi.SignedData) (*emptypb.Empty, error)
}

// UnimplementedNotifyServer should be embedded to have
// forward compatible implementations.
//
// NOTE: this should be embedded by value instead of pointer to avoid a nil
// pointer dereference when methods are called.
type UnimplementedNotifyServer struct{}

func (UnimplementedNotifyServer) Transaction(context.Context, *hi.SignedData) (*emptypb.Empty, error) {
	return nil, status.Error(codes.Unimplemented, "method Transaction not implemented")
}
func (UnimplementedNotifyServer) testEmbeddedByValue() {}

// UnsafeNotifyServer may be embedded to opt out of forward compatibility for this service.
// Use of this interface is not recommended, as added methods to NotifyServer will
// result in compilation errors.
type UnsafeNotifyServer interface {
	mustEmbedUnimplementedNotifyServer()
}

func RegisterNotifyServer(s grpc.ServiceRegistrar, srv NotifyServer) {
	// If the following call panics, it indicates UnimplementedNotifyServer was
	// embedded by pointer and is nil.  This will cause panics if an
	// unimplemented method is ever invoked, so we test this at initialization
	// time to prevent it from happening at runtime later due to I/O.
	if t, ok := srv.(interface{ testEmbeddedByValue() }); ok {
		t.testEmbeddedByValue()
	}
	s.RegisterService(&Notify_ServiceDesc, srv)
}

func _Notify_Transaction_Handler(srv interface{}, ctx context.Context, dec func(interface{}) error, interceptor grpc.UnaryServerInterceptor) (interface{}, error) {
	in := new(hi.SignedData)
	if err := dec(in); err != nil {
		return nil, err
	}
	if interceptor == nil {
		return srv.(NotifyServer).Transaction(ctx, in)
	}
	info := &grpc.UnaryServerInfo{
		Server:     srv,
		FullMethod: Notify_Transaction_FullMethodName,
	}
	handler := func(ctx context.Context, req interface{}) (interface{}, error) {
		return srv.(NotifyServer).Transaction(ctx, req.(*hi.SignedData))
	}
	return interceptor(ctx, in, info, handler)
}

// Notify_ServiceDesc is the grpc.ServiceDesc for Notify service.
// It's only intended for direct use with grpc.RegisterService,
// and not to be introspected or modified (even as a copy)
var Notify_ServiceDesc = grpc.ServiceDesc{
	ServiceName: "hi.did.Notify",
	HandlerType: (*NotifyServer)(nil),
	Methods: []grpc.MethodDesc{
		{
			MethodName: "Transaction",
			Handler:    _Notify_Transaction_Handler,
		},
	},
	Streams:  []grpc.StreamDesc{},
	Metadata: "hi/did/messaging.proto",
}
