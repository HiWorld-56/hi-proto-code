# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from google.protobuf import empty_pb2 as google_dot_protobuf_dot_empty__pb2
from hi.club import ledger_pb2 as hi_dot_club_dot_ledger__pb2


class LedgerStub(object):
    """── 机器人的资金流水 ─────────────────────────────────────────────────────────

    **只装非市场支出。** 市场订单(买插件、续费)有自己的一份 `MarketPayment`,
    那份挂在订单上、由认款流程写。同一笔记两处必然漂 —— 一处改了另一处不会知道。
    页面上要"一张总表"就在**上层合并两个来源**,不要在库里存第二份。

    第一种是**提款**:硬件机器人把自己收到的钱结算给主人。它不经市场、不产生订单,
    在此之前**全网没有任何地方看得到它** —— 出了事只能上链去翻。

    ## 为什么由机器人自己报

    付款方是机器人(私钥在它手里,club 全程碰不到钱),所以只有它知道"这笔发出去了"。
    用的是它自己的已认证通道 —— `payer` 天然就是它,不接受入参
    (让调用方传付款人,等于给自己开一个"以别人的名义记账"的口子)。

    ## 它是**留痕**,不是判据

    防重复付款那本账在机器人本地(sqlite,先落意向再动钱)。本表是给人看的:
    上报失败不能让钱卡住,所以机器人是**攒着补报**的 —— 于是同一条可能报两次,
    靠 `uuid` 幂等。**别拿本表当"付没付过"的依据**:它可能迟到,也可能永远不到
    (机器人在报出去之前就下线了)。
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.Record = channel.unary_unary(
                '/hi.club.Ledger/Record',
                request_serializer=hi_dot_club_dot_ledger__pb2.FundsRecord.SerializeToString,
                response_deserializer=google_dot_protobuf_dot_empty__pb2.Empty.FromString,
                _registered_method=True)
        self.List = channel.unary_unary(
                '/hi.club.Ledger/List',
                request_serializer=hi_dot_club_dot_ledger__pb2.ListFundsReq.SerializeToString,
                response_deserializer=hi_dot_club_dot_ledger__pb2.ListFundsResp.FromString,
                _registered_method=True)


class LedgerServicer(object):
    """── 机器人的资金流水 ─────────────────────────────────────────────────────────

    **只装非市场支出。** 市场订单(买插件、续费)有自己的一份 `MarketPayment`,
    那份挂在订单上、由认款流程写。同一笔记两处必然漂 —— 一处改了另一处不会知道。
    页面上要"一张总表"就在**上层合并两个来源**,不要在库里存第二份。

    第一种是**提款**:硬件机器人把自己收到的钱结算给主人。它不经市场、不产生订单,
    在此之前**全网没有任何地方看得到它** —— 出了事只能上链去翻。

    ## 为什么由机器人自己报

    付款方是机器人(私钥在它手里,club 全程碰不到钱),所以只有它知道"这笔发出去了"。
    用的是它自己的已认证通道 —— `payer` 天然就是它,不接受入参
    (让调用方传付款人,等于给自己开一个"以别人的名义记账"的口子)。

    ## 它是**留痕**,不是判据

    防重复付款那本账在机器人本地(sqlite,先落意向再动钱)。本表是给人看的:
    上报失败不能让钱卡住,所以机器人是**攒着补报**的 —— 于是同一条可能报两次,
    靠 `uuid` 幂等。**别拿本表当"付没付过"的依据**:它可能迟到,也可能永远不到
    (机器人在报出去之前就下线了)。
    """

    def Record(self, request, context):
        """机器人上报一笔自己花出去的钱。**幂等**:同 uuid 重复上报只更新,不新增。
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def List(self, request, context):
        """查流水。空 did = 看我自己;填了则**必须是我的仆从机器人**。
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_LedgerServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'Record': grpc.unary_unary_rpc_method_handler(
                    servicer.Record,
                    request_deserializer=hi_dot_club_dot_ledger__pb2.FundsRecord.FromString,
                    response_serializer=google_dot_protobuf_dot_empty__pb2.Empty.SerializeToString,
            ),
            'List': grpc.unary_unary_rpc_method_handler(
                    servicer.List,
                    request_deserializer=hi_dot_club_dot_ledger__pb2.ListFundsReq.FromString,
                    response_serializer=hi_dot_club_dot_ledger__pb2.ListFundsResp.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'hi.club.Ledger', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('hi.club.Ledger', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class Ledger(object):
    """── 机器人的资金流水 ─────────────────────────────────────────────────────────

    **只装非市场支出。** 市场订单(买插件、续费)有自己的一份 `MarketPayment`,
    那份挂在订单上、由认款流程写。同一笔记两处必然漂 —— 一处改了另一处不会知道。
    页面上要"一张总表"就在**上层合并两个来源**,不要在库里存第二份。

    第一种是**提款**:硬件机器人把自己收到的钱结算给主人。它不经市场、不产生订单,
    在此之前**全网没有任何地方看得到它** —— 出了事只能上链去翻。

    ## 为什么由机器人自己报

    付款方是机器人(私钥在它手里,club 全程碰不到钱),所以只有它知道"这笔发出去了"。
    用的是它自己的已认证通道 —— `payer` 天然就是它,不接受入参
    (让调用方传付款人,等于给自己开一个"以别人的名义记账"的口子)。

    ## 它是**留痕**,不是判据

    防重复付款那本账在机器人本地(sqlite,先落意向再动钱)。本表是给人看的:
    上报失败不能让钱卡住,所以机器人是**攒着补报**的 —— 于是同一条可能报两次,
    靠 `uuid` 幂等。**别拿本表当"付没付过"的依据**:它可能迟到,也可能永远不到
    (机器人在报出去之前就下线了)。
    """

    @staticmethod
    def Record(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.club.Ledger/Record',
            hi_dot_club_dot_ledger__pb2.FundsRecord.SerializeToString,
            google_dot_protobuf_dot_empty__pb2.Empty.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def List(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.club.Ledger/List',
            hi_dot_club_dot_ledger__pb2.ListFundsReq.SerializeToString,
            hi_dot_club_dot_ledger__pb2.ListFundsResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
