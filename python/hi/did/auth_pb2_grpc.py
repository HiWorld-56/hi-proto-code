# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from google.protobuf import empty_pb2 as google_dot_protobuf_dot_empty__pb2
from hi import common_pb2 as hi_dot_common__pb2
from hi.did import auth_pb2 as hi_dot_did_dot_auth__pb2


class AuthStub:
    """Auth —— 登录/登出。握手类是公开的(此时还没 token),身份确认类是 web3 验签(载荷带签名)。
    公开 与 web3验签 同处一个 service 是允许的(web3 本质是数据校验,不是方法鉴权)。

    ⭐ 客户端**只有 `Verify` 一个入口**(扫码、深链唤起都是它),分叉在后端。

    后端解出 web3 载荷后,**只拿 `req_id` 去查会话**,判据是**会话里的发起方 did**:
    是 hi-did 自己的哨兵(hisrv 这类自家后台把自己冒充成三方接进同一条流程)就自己发 token;
    是某个商户就只验签+转发、回拨商户 `LoginCallback`,token 由商户自己发。

    ⚠️ **载荷里的 `did` 字段没有任何地方读过**(hi-did 与 club 两侧都核过) —— 它只是发起方
    自报。客户端不必、也不该拿它替后端断言"这一定是三方":那正是 2026-08-18
    hidid-simple-app 报「需要注册为商户」的成因(截图粘贴来的码本质就是扫码,只有裸 reqId)。

    ⚠️ **冒充别的商户不成立**:商户自己也按 reqId 记一本账(如 club 的 `UserLogin` 第一步就查
    自己的会话),reqId 不是经它申请出来的,回拨那一步直接失败。两本账对不上就走不下去。

    ── `Notify` 的下场(2026-08-19,同一天删了两次)────────────────────────────
    它曾是"三方那半截"的单独入口,功能上是 `Verify` 的**子集**(只有三方那一支,
    还少了自家哨兵那一支)。08-18 第一次删,当天生产大面积「hidid 授权 hiclub 登录失败」——
    **不是删错了,是删早了**:已发布的 hidid-app 深链那条一直在调它。

    那次的真正教训是**它挂得静悄悄**:gRPC 未知方法在进拦截器**之前**就被 transport 拒,
    服务端一个字都不记 —— 日志里零错误,只有用户在报障。
    **删 rpc 之前先确认没有已发布的客户端在调;"日志里没见过"不算证据,调不通的恰恰不留痕迹。**

    现在客户端已经改成一律走 `Verify` 并验证可用,故正式删除,不留兼容入口。
    **不要再加回来。**
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.RefreshToken = channel.unary_unary(
                '/hi.did.Auth/RefreshToken',
                request_serializer=hi_dot_did_dot_auth__pb2.RefreshTokenReq.SerializeToString,
                response_deserializer=hi_dot_common__pb2.AuthToken.FromString,
                _registered_method=True)
        self.Verify = channel.unary_unary(
                '/hi.did.Auth/Verify',
                request_serializer=hi_dot_common__pb2.SignedData.SerializeToString,
                response_deserializer=hi_dot_did_dot_auth__pb2.LoginResp.FromString,
                _registered_method=True)
        self.VerifyOffline = channel.unary_unary(
                '/hi.did.Auth/VerifyOffline',
                request_serializer=hi_dot_common__pb2.SignedData.SerializeToString,
                response_deserializer=hi_dot_did_dot_auth__pb2.LoginResp.FromString,
                _registered_method=True)
        self.GenerateReqId = channel.unary_unary(
                '/hi.did.Auth/GenerateReqId',
                request_serializer=hi_dot_did_dot_auth__pb2.GenerateReqIdReq.SerializeToString,
                response_deserializer=hi_dot_common__pb2.RequestId.FromString,
                _registered_method=True)
        self.GetReqStatus = channel.unary_unary(
                '/hi.did.Auth/GetReqStatus',
                request_serializer=hi_dot_common__pb2.RequestId.SerializeToString,
                response_deserializer=hi_dot_did_dot_auth__pb2.ReqStatusResp.FromString,
                _registered_method=True)
        self.Logout = channel.unary_unary(
                '/hi.did.Auth/Logout',
                request_serializer=hi_dot_common__pb2.SignedData.SerializeToString,
                response_deserializer=google_dot_protobuf_dot_empty__pb2.Empty.FromString,
                _registered_method=True)


class AuthServicer:
    """Auth —— 登录/登出。握手类是公开的(此时还没 token),身份确认类是 web3 验签(载荷带签名)。
    公开 与 web3验签 同处一个 service 是允许的(web3 本质是数据校验,不是方法鉴权)。

    ⭐ 客户端**只有 `Verify` 一个入口**(扫码、深链唤起都是它),分叉在后端。

    后端解出 web3 载荷后,**只拿 `req_id` 去查会话**,判据是**会话里的发起方 did**:
    是 hi-did 自己的哨兵(hisrv 这类自家后台把自己冒充成三方接进同一条流程)就自己发 token;
    是某个商户就只验签+转发、回拨商户 `LoginCallback`,token 由商户自己发。

    ⚠️ **载荷里的 `did` 字段没有任何地方读过**(hi-did 与 club 两侧都核过) —— 它只是发起方
    自报。客户端不必、也不该拿它替后端断言"这一定是三方":那正是 2026-08-18
    hidid-simple-app 报「需要注册为商户」的成因(截图粘贴来的码本质就是扫码,只有裸 reqId)。

    ⚠️ **冒充别的商户不成立**:商户自己也按 reqId 记一本账(如 club 的 `UserLogin` 第一步就查
    自己的会话),reqId 不是经它申请出来的,回拨那一步直接失败。两本账对不上就走不下去。

    ── `Notify` 的下场(2026-08-19,同一天删了两次)────────────────────────────
    它曾是"三方那半截"的单独入口,功能上是 `Verify` 的**子集**(只有三方那一支,
    还少了自家哨兵那一支)。08-18 第一次删,当天生产大面积「hidid 授权 hiclub 登录失败」——
    **不是删错了,是删早了**:已发布的 hidid-app 深链那条一直在调它。

    那次的真正教训是**它挂得静悄悄**:gRPC 未知方法在进拦截器**之前**就被 transport 拒,
    服务端一个字都不记 —— 日志里零错误,只有用户在报障。
    **删 rpc 之前先确认没有已发布的客户端在调;"日志里没见过"不算证据,调不通的恰恰不留痕迹。**

    现在客户端已经改成一律走 `Verify` 并验证可用,故正式删除,不留兼容入口。
    **不要再加回来。**
    """

    def RefreshToken(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def Verify(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def VerifyOffline(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def GenerateReqId(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def GetReqStatus(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def Logout(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_AuthServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'RefreshToken': grpc.unary_unary_rpc_method_handler(
                    servicer.RefreshToken,
                    request_deserializer=hi_dot_did_dot_auth__pb2.RefreshTokenReq.FromString,
                    response_serializer=hi_dot_common__pb2.AuthToken.SerializeToString,
            ),
            'Verify': grpc.unary_unary_rpc_method_handler(
                    servicer.Verify,
                    request_deserializer=hi_dot_common__pb2.SignedData.FromString,
                    response_serializer=hi_dot_did_dot_auth__pb2.LoginResp.SerializeToString,
            ),
            'VerifyOffline': grpc.unary_unary_rpc_method_handler(
                    servicer.VerifyOffline,
                    request_deserializer=hi_dot_common__pb2.SignedData.FromString,
                    response_serializer=hi_dot_did_dot_auth__pb2.LoginResp.SerializeToString,
            ),
            'GenerateReqId': grpc.unary_unary_rpc_method_handler(
                    servicer.GenerateReqId,
                    request_deserializer=hi_dot_did_dot_auth__pb2.GenerateReqIdReq.FromString,
                    response_serializer=hi_dot_common__pb2.RequestId.SerializeToString,
            ),
            'GetReqStatus': grpc.unary_unary_rpc_method_handler(
                    servicer.GetReqStatus,
                    request_deserializer=hi_dot_common__pb2.RequestId.FromString,
                    response_serializer=hi_dot_did_dot_auth__pb2.ReqStatusResp.SerializeToString,
            ),
            'Logout': grpc.unary_unary_rpc_method_handler(
                    servicer.Logout,
                    request_deserializer=hi_dot_common__pb2.SignedData.FromString,
                    response_serializer=google_dot_protobuf_dot_empty__pb2.Empty.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'hi.did.Auth', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('hi.did.Auth', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class Auth:
    """Auth —— 登录/登出。握手类是公开的(此时还没 token),身份确认类是 web3 验签(载荷带签名)。
    公开 与 web3验签 同处一个 service 是允许的(web3 本质是数据校验,不是方法鉴权)。

    ⭐ 客户端**只有 `Verify` 一个入口**(扫码、深链唤起都是它),分叉在后端。

    后端解出 web3 载荷后,**只拿 `req_id` 去查会话**,判据是**会话里的发起方 did**:
    是 hi-did 自己的哨兵(hisrv 这类自家后台把自己冒充成三方接进同一条流程)就自己发 token;
    是某个商户就只验签+转发、回拨商户 `LoginCallback`,token 由商户自己发。

    ⚠️ **载荷里的 `did` 字段没有任何地方读过**(hi-did 与 club 两侧都核过) —— 它只是发起方
    自报。客户端不必、也不该拿它替后端断言"这一定是三方":那正是 2026-08-18
    hidid-simple-app 报「需要注册为商户」的成因(截图粘贴来的码本质就是扫码,只有裸 reqId)。

    ⚠️ **冒充别的商户不成立**:商户自己也按 reqId 记一本账(如 club 的 `UserLogin` 第一步就查
    自己的会话),reqId 不是经它申请出来的,回拨那一步直接失败。两本账对不上就走不下去。

    ── `Notify` 的下场(2026-08-19,同一天删了两次)────────────────────────────
    它曾是"三方那半截"的单独入口,功能上是 `Verify` 的**子集**(只有三方那一支,
    还少了自家哨兵那一支)。08-18 第一次删,当天生产大面积「hidid 授权 hiclub 登录失败」——
    **不是删错了,是删早了**:已发布的 hidid-app 深链那条一直在调它。

    那次的真正教训是**它挂得静悄悄**:gRPC 未知方法在进拦截器**之前**就被 transport 拒,
    服务端一个字都不记 —— 日志里零错误,只有用户在报障。
    **删 rpc 之前先确认没有已发布的客户端在调;"日志里没见过"不算证据,调不通的恰恰不留痕迹。**

    现在客户端已经改成一律走 `Verify` 并验证可用,故正式删除,不留兼容入口。
    **不要再加回来。**
    """

    @staticmethod
    def RefreshToken(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Auth/RefreshToken',
            hi_dot_did_dot_auth__pb2.RefreshTokenReq.SerializeToString,
            hi_dot_common__pb2.AuthToken.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def Verify(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Auth/Verify',
            hi_dot_common__pb2.SignedData.SerializeToString,
            hi_dot_did_dot_auth__pb2.LoginResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def VerifyOffline(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Auth/VerifyOffline',
            hi_dot_common__pb2.SignedData.SerializeToString,
            hi_dot_did_dot_auth__pb2.LoginResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def GenerateReqId(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Auth/GenerateReqId',
            hi_dot_did_dot_auth__pb2.GenerateReqIdReq.SerializeToString,
            hi_dot_common__pb2.RequestId.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def GetReqStatus(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Auth/GetReqStatus',
            hi_dot_common__pb2.RequestId.SerializeToString,
            hi_dot_did_dot_auth__pb2.ReqStatusResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def Logout(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Auth/Logout',
            hi_dot_common__pb2.SignedData.SerializeToString,
            google_dot_protobuf_dot_empty__pb2.Empty.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
