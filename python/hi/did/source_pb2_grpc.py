# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from google.protobuf import empty_pb2 as google_dot_protobuf_dot_empty__pb2
from hi import common_pb2 as hi_dot_common__pb2


class SourceStub(object):
    """Source —— hidid 侧直接搬运二进制的方法。分法见 hi/club/source.proto 的说明。

    **所有身份实体的头像都落 hidid/avatar/**:用户、商户(logo 即商户头像)、机器人,
    不再按实体分目录 —— 它们在存储上是同一类东西,分开只是徒增分支。
    以前不分家(全塞 upload 的按月目录)出过批量误删头像的事故,那是"不该合的合了";
    这里是"不该分的分了",两回事。

    ⚠️ 只回 url,**不改资料** —— 落库分别走 `User.Edit` / `Merchant.Update` /
    `Merchant.SetUsers`,上传与落库解耦(这个约定原先就是这样,只是方法搬了家)。
    ⚠️ **头像没有商户档的上传口**,也不需要:资源的消耗方不关心资源由谁上传。
    商户要给名下用户设头像,拿任意一个自己有权调的上传口换到 url,再走
    `Merchant.SetUsers` 即可 —— 设置那步才是有主体校验的地方。
    公共区(`hidid/pub/`)同理**只有用户档**:它是通用口子(哪儿要直链都能用),
    没必要为某个调用方的身份再开一档 —— 曾短暂拆出过 `SourceMerchant`,
    发现除了让同一件事有两个入口之外没带来任何东西,删了。
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.UploadAvatar = channel.unary_unary(
                '/hi.did.Source/UploadAvatar',
                request_serializer=hi_dot_common__pb2.UploadReq.SerializeToString,
                response_deserializer=hi_dot_common__pb2.UploadResp.FromString,
                _registered_method=True)
        self.UploadLog = channel.unary_unary(
                '/hi.did.Source/UploadLog',
                request_serializer=hi_dot_common__pb2.UploadReq.SerializeToString,
                response_deserializer=hi_dot_common__pb2.UploadResp.FromString,
                _registered_method=True)
        self.UploadPub = channel.unary_unary(
                '/hi.did.Source/UploadPub',
                request_serializer=hi_dot_common__pb2.UploadReq.SerializeToString,
                response_deserializer=hi_dot_common__pb2.UploadResp.FromString,
                _registered_method=True)
        self.Delete = channel.unary_unary(
                '/hi.did.Source/Delete',
                request_serializer=hi_dot_common__pb2.DeleteResourceReq.SerializeToString,
                response_deserializer=google_dot_protobuf_dot_empty__pb2.Empty.FromString,
                _registered_method=True)


class SourceServicer(object):
    """Source —— hidid 侧直接搬运二进制的方法。分法见 hi/club/source.proto 的说明。

    **所有身份实体的头像都落 hidid/avatar/**:用户、商户(logo 即商户头像)、机器人,
    不再按实体分目录 —— 它们在存储上是同一类东西,分开只是徒增分支。
    以前不分家(全塞 upload 的按月目录)出过批量误删头像的事故,那是"不该合的合了";
    这里是"不该分的分了",两回事。

    ⚠️ 只回 url,**不改资料** —— 落库分别走 `User.Edit` / `Merchant.Update` /
    `Merchant.SetUsers`,上传与落库解耦(这个约定原先就是这样,只是方法搬了家)。
    ⚠️ **头像没有商户档的上传口**,也不需要:资源的消耗方不关心资源由谁上传。
    商户要给名下用户设头像,拿任意一个自己有权调的上传口换到 url,再走
    `Merchant.SetUsers` 即可 —— 设置那步才是有主体校验的地方。
    公共区(`hidid/pub/`)同理**只有用户档**:它是通用口子(哪儿要直链都能用),
    没必要为某个调用方的身份再开一档 —— 曾短暂拆出过 `SourceMerchant`,
    发现除了让同一件事有两个入口之外没带来任何东西,删了。
    """

    def UploadAvatar(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def UploadLog(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def UploadPub(self, request, context):
        """公共区上传 → `hidid/pub/`,**拿到 url 直接 http get 就能下**(桶是永固公开读,
        与 hiclub/pub、hiai/pub 同一口径)。给"前端要个能直链的地址"这类需求用:
        商户后台传个附件/图、端上传个要分享出去的文件,拿回 url 就完事,不用再走鉴权下载口。

        ⚠️ **公开即公开**:url 是 32 位随机名,知道 url 就能拿到内容,别往这儿放私密文件
        (私密的走各自的私有桶 + 鉴权下载口)。
        ⚠️ 只回 url,**不改任何资料** —— 与本文件其它上传口一致,落库各走各的设置方法;
        设置失败记得调 Delete 收尸,否则就是无主文件(公开桶没有 lifecycle 兜底)。

        本 service 里**只有它导出 http 路由**(网页要传);其余三个是端上的活,只走 grpc
        (`Delete` 尤其不导出:它按 url 删对象、不做归属校验,挂到网关上风险不对等)。
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')

    def Delete(self, request, context):
        """Delete 删掉刚传上去、但**没被任何地方引用**的对象。

        上传与落库解耦之后必然产生这个缺口:上传成功 → 调设置方法 → 设置失败,
        那个对象就成了无主文件,永久桶又没有 lifecycle 兜底。约定:

        上传 → 拿 url 调设置方法 → 设置失败 → **立即调 Delete**

        ⚠️ 不做归属校验,和上传对称 —— url 是 32 位随机名,知道 url 本身就是凭据。
        这也意味着**它删得掉任何你知道 url 的对象**,别把 url 泄漏出去。
        """
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_SourceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'UploadAvatar': grpc.unary_unary_rpc_method_handler(
                    servicer.UploadAvatar,
                    request_deserializer=hi_dot_common__pb2.UploadReq.FromString,
                    response_serializer=hi_dot_common__pb2.UploadResp.SerializeToString,
            ),
            'UploadLog': grpc.unary_unary_rpc_method_handler(
                    servicer.UploadLog,
                    request_deserializer=hi_dot_common__pb2.UploadReq.FromString,
                    response_serializer=hi_dot_common__pb2.UploadResp.SerializeToString,
            ),
            'UploadPub': grpc.unary_unary_rpc_method_handler(
                    servicer.UploadPub,
                    request_deserializer=hi_dot_common__pb2.UploadReq.FromString,
                    response_serializer=hi_dot_common__pb2.UploadResp.SerializeToString,
            ),
            'Delete': grpc.unary_unary_rpc_method_handler(
                    servicer.Delete,
                    request_deserializer=hi_dot_common__pb2.DeleteResourceReq.FromString,
                    response_serializer=google_dot_protobuf_dot_empty__pb2.Empty.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'hi.did.Source', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('hi.did.Source', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class Source(object):
    """Source —— hidid 侧直接搬运二进制的方法。分法见 hi/club/source.proto 的说明。

    **所有身份实体的头像都落 hidid/avatar/**:用户、商户(logo 即商户头像)、机器人,
    不再按实体分目录 —— 它们在存储上是同一类东西,分开只是徒增分支。
    以前不分家(全塞 upload 的按月目录)出过批量误删头像的事故,那是"不该合的合了";
    这里是"不该分的分了",两回事。

    ⚠️ 只回 url,**不改资料** —— 落库分别走 `User.Edit` / `Merchant.Update` /
    `Merchant.SetUsers`,上传与落库解耦(这个约定原先就是这样,只是方法搬了家)。
    ⚠️ **头像没有商户档的上传口**,也不需要:资源的消耗方不关心资源由谁上传。
    商户要给名下用户设头像,拿任意一个自己有权调的上传口换到 url,再走
    `Merchant.SetUsers` 即可 —— 设置那步才是有主体校验的地方。
    公共区(`hidid/pub/`)同理**只有用户档**:它是通用口子(哪儿要直链都能用),
    没必要为某个调用方的身份再开一档 —— 曾短暂拆出过 `SourceMerchant`,
    发现除了让同一件事有两个入口之外没带来任何东西,删了。
    """

    @staticmethod
    def UploadAvatar(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Source/UploadAvatar',
            hi_dot_common__pb2.UploadReq.SerializeToString,
            hi_dot_common__pb2.UploadResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def UploadLog(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Source/UploadLog',
            hi_dot_common__pb2.UploadReq.SerializeToString,
            hi_dot_common__pb2.UploadResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def UploadPub(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Source/UploadPub',
            hi_dot_common__pb2.UploadReq.SerializeToString,
            hi_dot_common__pb2.UploadResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)

    @staticmethod
    def Delete(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Source/Delete',
            hi_dot_common__pb2.DeleteResourceReq.SerializeToString,
            google_dot_protobuf_dot_empty__pb2.Empty.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
