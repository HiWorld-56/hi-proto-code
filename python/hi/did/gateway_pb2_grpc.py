# Generated by the gRPC Python protocol compiler plugin. DO NOT EDIT!
"""Client and server classes corresponding to protobuf-defined services."""
import grpc

from google.protobuf import empty_pb2 as google_dot_protobuf_dot_empty__pb2
from hi import common_pb2 as hi_dot_common__pb2
from hi.did import gateway_pb2 as hi_dot_did_dot_gateway__pb2


class GatewayStub:
    """── 网关配置(区块链节点 url + api_key)──────────────────────────────────────
    这是**给前端用的配置**:hidid app 里的 hidid-core、以及 hiclub(内嵌 hidid-core)
    都要拿它去连区块链节点。resp 里的 api_key 是**共享的节点访问凭证**(非用户私密),
    前端连节点必须带,故读侧对已登录用户/商户开放。

    ⚠️ 后端(开发/生产)不要走这个 RPC 取配置 —— 生产与开发/前端环境不同,
    后端应从自己的配置文件引入。

    读写拆两个 service(同 service 档位必须一致),照 DApp / DAppAdmin 的既有范式:
    Gateway(读,用户或商户) / GatewayAdmin(写,超管)。
    HTTP 路径随之改为 /api/v1/gateway/list(读)与 /api/v1/gateway_admin/set(写)——
    URL 与 selector 同构;前端调用地址已从旧的 gateway_config/* 变更,需同步更新。

    网关配置(客户端读)。主体=网关配置这份数据,谁登录了都要读:
    app 用户(AUTH_USER)、hiclub 商户(AUTH_MERCHANT);超管运维页也走这个读(它是用户)。
    本方法**不依赖"我是谁"**,故两个主体都收 —— 标两行,任一通过即放行。
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.List = channel.unary_unary(
                '/hi.did.Gateway/List',
                request_serializer=google_dot_protobuf_dot_empty__pb2.Empty.SerializeToString,
                response_deserializer=hi_dot_did_dot_gateway__pb2.GatewayConfigListResp.FromString,
                _registered_method=True)


class GatewayServicer:
    """── 网关配置(区块链节点 url + api_key)──────────────────────────────────────
    这是**给前端用的配置**:hidid app 里的 hidid-core、以及 hiclub(内嵌 hidid-core)
    都要拿它去连区块链节点。resp 里的 api_key 是**共享的节点访问凭证**(非用户私密),
    前端连节点必须带,故读侧对已登录用户/商户开放。

    ⚠️ 后端(开发/生产)不要走这个 RPC 取配置 —— 生产与开发/前端环境不同,
    后端应从自己的配置文件引入。

    读写拆两个 service(同 service 档位必须一致),照 DApp / DAppAdmin 的既有范式:
    Gateway(读,用户或商户) / GatewayAdmin(写,超管)。
    HTTP 路径随之改为 /api/v1/gateway/list(读)与 /api/v1/gateway_admin/set(写)——
    URL 与 selector 同构;前端调用地址已从旧的 gateway_config/* 变更,需同步更新。

    网关配置(客户端读)。主体=网关配置这份数据,谁登录了都要读:
    app 用户(AUTH_USER)、hiclub 商户(AUTH_MERCHANT);超管运维页也走这个读(它是用户)。
    本方法**不依赖"我是谁"**,故两个主体都收 —— 标两行,任一通过即放行。
    """

    def List(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_GatewayServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'List': grpc.unary_unary_rpc_method_handler(
                    servicer.List,
                    request_deserializer=google_dot_protobuf_dot_empty__pb2.Empty.FromString,
                    response_serializer=hi_dot_did_dot_gateway__pb2.GatewayConfigListResp.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'hi.did.Gateway', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('hi.did.Gateway', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class Gateway:
    """── 网关配置(区块链节点 url + api_key)──────────────────────────────────────
    这是**给前端用的配置**:hidid app 里的 hidid-core、以及 hiclub(内嵌 hidid-core)
    都要拿它去连区块链节点。resp 里的 api_key 是**共享的节点访问凭证**(非用户私密),
    前端连节点必须带,故读侧对已登录用户/商户开放。

    ⚠️ 后端(开发/生产)不要走这个 RPC 取配置 —— 生产与开发/前端环境不同,
    后端应从自己的配置文件引入。

    读写拆两个 service(同 service 档位必须一致),照 DApp / DAppAdmin 的既有范式:
    Gateway(读,用户或商户) / GatewayAdmin(写,超管)。
    HTTP 路径随之改为 /api/v1/gateway/list(读)与 /api/v1/gateway_admin/set(写)——
    URL 与 selector 同构;前端调用地址已从旧的 gateway_config/* 变更,需同步更新。

    网关配置(客户端读)。主体=网关配置这份数据,谁登录了都要读:
    app 用户(AUTH_USER)、hiclub 商户(AUTH_MERCHANT);超管运维页也走这个读(它是用户)。
    本方法**不依赖"我是谁"**,故两个主体都收 —— 标两行,任一通过即放行。
    """

    @staticmethod
    def List(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.Gateway/List',
            google_dot_protobuf_dot_empty__pb2.Empty.SerializeToString,
            hi_dot_did_dot_gateway__pb2.GatewayConfigListResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)


class GatewayDeviceStub:
    """网关配置(**设备读**)。与 Gateway 同一份数据,差别只在**主体怎么证明自己**:

    · `Gateway.List`      —— 主体是**登录用户/商户**,拿 token(web、app 登录后)。
    · `GatewayDevice.List` —— 主体是**设备**:它有私钥、**没有登录态**。

    机器人(hinj-brain)就是后者:它跟 hi-did 之间只走验签接口(UpdateAddresses / Pay.Notify),
    从来没有 token。而端点与 api_key 又必须从服务端取 —— 写死在固件里意味着
    换节点要重新 OTA,key 还会随镜像发到每台设备上。

    ⚠️ 档位不同就必须拆 service(同 service 档位必须一致),与
    Gateway / GatewayAdmin、DApp / DAppAdmin 同一个范式。

    载荷(SignedData.Data)不带任何字段(`{}` 即可)—— 这个方法**不依赖"我是谁"**,
    签名只用来证明"是一台持私钥的设备",挡住匿名抓取节点凭证。
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.List = channel.unary_unary(
                '/hi.did.GatewayDevice/List',
                request_serializer=hi_dot_common__pb2.SignedData.SerializeToString,
                response_deserializer=hi_dot_did_dot_gateway__pb2.GatewayConfigListResp.FromString,
                _registered_method=True)


class GatewayDeviceServicer:
    """网关配置(**设备读**)。与 Gateway 同一份数据,差别只在**主体怎么证明自己**:

    · `Gateway.List`      —— 主体是**登录用户/商户**,拿 token(web、app 登录后)。
    · `GatewayDevice.List` —— 主体是**设备**:它有私钥、**没有登录态**。

    机器人(hinj-brain)就是后者:它跟 hi-did 之间只走验签接口(UpdateAddresses / Pay.Notify),
    从来没有 token。而端点与 api_key 又必须从服务端取 —— 写死在固件里意味着
    换节点要重新 OTA,key 还会随镜像发到每台设备上。

    ⚠️ 档位不同就必须拆 service(同 service 档位必须一致),与
    Gateway / GatewayAdmin、DApp / DAppAdmin 同一个范式。

    载荷(SignedData.Data)不带任何字段(`{}` 即可)—— 这个方法**不依赖"我是谁"**,
    签名只用来证明"是一台持私钥的设备",挡住匿名抓取节点凭证。
    """

    def List(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_GatewayDeviceServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'List': grpc.unary_unary_rpc_method_handler(
                    servicer.List,
                    request_deserializer=hi_dot_common__pb2.SignedData.FromString,
                    response_serializer=hi_dot_did_dot_gateway__pb2.GatewayConfigListResp.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'hi.did.GatewayDevice', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('hi.did.GatewayDevice', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class GatewayDevice:
    """网关配置(**设备读**)。与 Gateway 同一份数据,差别只在**主体怎么证明自己**:

    · `Gateway.List`      —— 主体是**登录用户/商户**,拿 token(web、app 登录后)。
    · `GatewayDevice.List` —— 主体是**设备**:它有私钥、**没有登录态**。

    机器人(hinj-brain)就是后者:它跟 hi-did 之间只走验签接口(UpdateAddresses / Pay.Notify),
    从来没有 token。而端点与 api_key 又必须从服务端取 —— 写死在固件里意味着
    换节点要重新 OTA,key 还会随镜像发到每台设备上。

    ⚠️ 档位不同就必须拆 service(同 service 档位必须一致),与
    Gateway / GatewayAdmin、DApp / DAppAdmin 同一个范式。

    载荷(SignedData.Data)不带任何字段(`{}` 即可)—— 这个方法**不依赖"我是谁"**,
    签名只用来证明"是一台持私钥的设备",挡住匿名抓取节点凭证。
    """

    @staticmethod
    def List(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.GatewayDevice/List',
            hi_dot_common__pb2.SignedData.SerializeToString,
            hi_dot_did_dot_gateway__pb2.GatewayConfigListResp.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)


class GatewayAdminStub:
    """网关配置维护(超管写)。纯内部运维:改区块链节点 url + api_key。
    """

    def __init__(self, channel):
        """Constructor.

        Args:
            channel: A grpc.Channel.
        """
        self.Set = channel.unary_unary(
                '/hi.did.GatewayAdmin/Set',
                request_serializer=hi_dot_did_dot_gateway__pb2.GatewayConfigSetReq.SerializeToString,
                response_deserializer=google_dot_protobuf_dot_empty__pb2.Empty.FromString,
                _registered_method=True)


class GatewayAdminServicer:
    """网关配置维护(超管写)。纯内部运维:改区块链节点 url + api_key。
    """

    def Set(self, request, context):
        """Missing associated documentation comment in .proto file."""
        context.set_code(grpc.StatusCode.UNIMPLEMENTED)
        context.set_details('Method not implemented!')
        raise NotImplementedError('Method not implemented!')


def add_GatewayAdminServicer_to_server(servicer, server):
    rpc_method_handlers = {
            'Set': grpc.unary_unary_rpc_method_handler(
                    servicer.Set,
                    request_deserializer=hi_dot_did_dot_gateway__pb2.GatewayConfigSetReq.FromString,
                    response_serializer=google_dot_protobuf_dot_empty__pb2.Empty.SerializeToString,
            ),
    }
    generic_handler = grpc.method_handlers_generic_handler(
            'hi.did.GatewayAdmin', rpc_method_handlers)
    server.add_generic_rpc_handlers((generic_handler,))
    server.add_registered_method_handlers('hi.did.GatewayAdmin', rpc_method_handlers)


 # This class is part of an EXPERIMENTAL API.
class GatewayAdmin:
    """网关配置维护(超管写)。纯内部运维:改区块链节点 url + api_key。
    """

    @staticmethod
    def Set(request,
            target,
            options=(),
            channel_credentials=None,
            call_credentials=None,
            insecure=False,
            compression=None,
            wait_for_ready=None,
            timeout=None,
            metadata=None):
        return grpc.experimental.unary_unary(
            request,
            target,
            '/hi.did.GatewayAdmin/Set',
            hi_dot_did_dot_gateway__pb2.GatewayConfigSetReq.SerializeToString,
            google_dot_protobuf_dot_empty__pb2.Empty.FromString,
            options,
            channel_credentials,
            insecure,
            call_credentials,
            compression,
            wait_for_ready,
            timeout,
            metadata,
            _registered_method=True)
